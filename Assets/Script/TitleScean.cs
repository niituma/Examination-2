using UnityEngine;
using UnityEngine.SceneManagement;

public class TitleScean : MonoBehaviour
{
    // Update is called once per frame
    void Update()
    {
        SceneManager.LoadScene("Title");
    }
}
